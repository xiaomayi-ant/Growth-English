# EnPet 开发任务清单

## 0. 当前进度快照

更新时间：2026-08-04

- MVP 纵向链路已完成：词库导入、SQLite 状态、固定复习轮次、本地 API、React 前端、Obsidian 入口和 Markdown 快照。
- 真实词库已导入：`english-words.md` 150 个词条，`english-words-002.md` 110 个词条，共 260 个词条。
- 已创建 2026-07-15 的新词会话，选中 6 个词；该会话尚未完成，因此尚未生成后续复习轮次。
- 前端 UI 已做一轮规范修复：状态文案中文化、移除内部 ID 展示、修复 fieldset 边距、字体栈补 CJK 回退。
- 新增 `apps/desktop`（Electron 43 壳）：Fastify 服务端以 esbuild 单文件 bundle 在 Electron 主进程内运行，BrowserWindow 加载 loopback 地址；`node:sqlite` 在 Electron 内置 Node 22 中直接可用，无需 flag 和原生模块重编译。
- 桌面版数据库位于 `~/Library/Application Support/EnPet/enpet.sqlite3`；首次启动自动迁移旧 `data/enpet.sqlite3`（含 `-wal`/`-shm`，WAL 不完整复制的坑已修复并验证 260 词条完整迁移）。
- 打包链路已通：`pnpm release` 一条命令产出未签名 dmg（arm64/x86_64）；`.github/workflows/release.yml` 支持推 `v*` tag 自动构建并上传到 GitHub Releases 草稿。
- 已验证命令：`pnpm test`、`pnpm typecheck`、`pnpm lint`、`pnpm build`、`pnpm release`。
- 未完成重点：真实语义分组、自然短文生成、开放式答案评测、工作日自动化、复习上限和运行时间等产品参数；桌面端待办（苹果签名公证、设置页、自定义图标、自动更新）。

## 1. 开发原则

- 前端、后端和共享领域代码统一使用 TypeScript。
- SQLite 是学习状态的唯一事实来源。
- 原始 `english-words*.md` 始终只读。
- 新词学习和到期复习是两种独立任务、会话和页面。
- 每完成一个里程碑，都必须具备可运行结果和自动化测试。
- 优先完成一条最小纵向链路，再扩充题型和视觉细节。

## 2. 推荐技术基线

以下作为首选方案，除非开发前发现明确阻碍：

- Node.js 22 LTS
- pnpm workspace
- TypeScript strict mode
- Fastify 本地 API
- React + Vite 前端
- `better-sqlite3` + SQL migration 文件
- Zod 共享数据校验
- Vitest 单元与集成测试
- Playwright 端到端测试
- Biome 格式化和静态检查
- `date-fns` 处理日期和工作日

应用只监听 `127.0.0.1`。首期不引入容器、云数据库和完整 Obsidian 插件。

## 3. 里程碑总览

| 里程碑 | 结果 |
| --- | --- |
| M0 | 未决产品规则形成明确配置 |
| M1 | TypeScript 工程可构建、测试和启动 |
| M2 | 领域模型和日期规则可独立验证 |
| M3 | Markdown 词库可稳定导入 SQLite |
| M4 | 新词与复习状态机完整可测 |
| M5 | 本地 API 提供完整学习能力 |
| M6 | 最小前端完成新词和复习闭环 |
| M7 | Codex 生成短文并评测开放式答案 |
| M8 | Obsidian 能显示应用和学习归档 |
| M9 | 两个工作日定时任务稳定运行 |
| M10 | 真实词库试运行并具备恢复能力 |

## 4. M0：确认实现参数

这些事项不会阻止工程初始化，但必须在对应模块编码前确定。

- [x] 确认每日新词默认数量，当前建议 `6`。
- [ ] 确认每日复习数量是否设上限及上限值。
- [ ] 确认新词任务的工作日运行时间。
- [ ] 确认复习任务的工作日运行时间。
- [ ] 确认短文是否允许自然加入复习词。
- [ ] 确认评分由用户自评、系统评测还是组合产生。
- [ ] 确认 D21 未作答时标记为未掌握还是继续等待。
- [ ] 确认 Codex 接入方式：Codex 自动化、Codex CLI 或 OpenAI API。
- [ ] 确认 SQLite 自动备份频率和保留天数。

完成标准：所有决定进入类型化配置和架构文档，不散落在代码常量中。

## 5. M1：初始化 TypeScript 工程

### 5.1 项目结构

- [x] 初始化 Git 仓库和根目录 `.gitignore`。
- [x] 初始化 pnpm workspace 和根 `package.json`。
- [x] 建立 `apps/web`、`apps/server` 和 `packages` 目录。
- [x] 建立 `packages/core`，放置共享领域类型和规则。
- [x] 建立 `packages/database`、`packages/vocabulary-import`、`packages/scheduler`。
- [x] 建立 `packages/evaluation` 和 `packages/reporting`。
- [ ] 建立 `data`、`backups`、`tests/fixtures`，并忽略运行时文件。

### 5.2 工程质量

- [ ] 配置 TypeScript strict mode 和 workspace path aliases。
- [x] 配置 Biome 检查和格式化。
- [ ] 配置 Vitest、覆盖率和测试脚本。
- [x] 配置前后端统一的环境变量解析。
- [x] 添加 `dev`、`build`、`test`、`lint`、`typecheck` 命令。
- [x] 添加健康检查占位服务和最小前端页面。

完成标准：一次命令可启动前后端；构建、类型检查、静态检查和空测试全部通过。

## 6. M2：定义领域模型和固定规则

### 6.1 共享类型

- [ ] 定义 `SourceEntryId`，格式为 `f001-r001-c01`。
- [ ] 定义 `SourceEntry`、`LearningItem`、`ReviewRound`。
- [ ] 定义 `StudySession`、`GeneratedExercise`、`EvaluationResult`。
- [x] 定义 `new_learning` 和 `review` 两种会话类型。
- [x] 定义 `again`、`hard`、`good`、`easy` 评分枚举。
- [ ] 定义 API 请求和响应 Zod schema。

### 6.2 日期规则

- [x] 实现 D1、D3、D7、D14、D21 固定偏移。
- [x] 实现周六、周日顺延到下一个工作日。
- [x] 实现同一词同一天多个到期轮次合并规则。
- [x] 实现到期、逾期、即将到期状态计算。
- [x] 支持测试注入当前日期，禁止业务代码直接依赖系统时间。

### 6.3 配置

- [x] 定义词库路径、数据库路径、报告路径和监听端口。
- [x] 定义每日新词数量和复习数量上限。
- [x] 定义时区，默认 `Asia/Shanghai`。
- [ ] 校验目录不存在、路径不可写和配置非法等错误。

完成标准：不连接数据库也能通过单元测试验证 ID、状态和全部日期边界。

## 7. M3：Markdown 导入和 SQLite

### 7.1 Markdown 解析

- [x] 发现并排序 `english-words.md`、`english-words-002.md` 等文件。
- [x] 把无编号文件映射为逻辑文档 1。
- [x] 按表格正文行和列稳定生成位置 ID。
- [x] 解析 `word<br>中文<br>phonetic` 单元格。
- [x] 正确处理转义竖线、空单元格、缺失音标和短语。
- [x] 保留重复英文词，不执行文本去重。
- [x] 对格式损坏的行返回可定位错误，不静默跳过。
- [ ] 建立覆盖真实格式的 fixture 和解析测试。

### 7.2 SQLite schema

- [x] 建立 migration runner 和 schema version 表。
- [x] 创建 `source_entries` 表和位置唯一约束。
- [x] 创建 `learning_items` 表。
- [x] 创建 `review_rounds` 表。
- [x] 创建 `study_sessions` 表，并对日期加会话类型唯一约束。
- [x] 创建 `generated_exercises` 表。
- [x] 创建答案、评分和评测反馈所需字段或关联表。
- [ ] 为到期查询、当前文档查询和历史查询建立索引。

### 7.3 导入服务

- [x] 使用来源位置执行幂等 upsert。
- [x] 更新词义或音标时保留已有学习历史。
- [ ] 原始文件删除词条时先标记，不自动删除学习记录。
- [x] 使用事务避免部分文件导入成功。
- [ ] 提供 dry-run 导入摘要。
- [x] 提供导入数量、变更数量和错误日志。

完成标准：重复导入同一批文件不会增加重复记录，且 150 个词条的位置保持稳定。

## 8. M4：实现学习和复习状态机

### 8.1 当前文档进度

- [x] 查询第一个尚未全部首次学习的文档。
- [x] 查询当前文档中所有未学词。
- [x] 当前文档完成后自动切换到下一编号文档。
- [x] 当前文档仍在追加词条时能够重新识别新增位置。

### 8.2 新词学习任务

- [x] 创建每天唯一的 `new_learning` 会话。
- [x] 从当前文档未学词中准备可供 Codex 选择的列表。
- [x] 接受 Codex 返回的最多 6 个来源 ID，并验证都属于当前文档且尚未学习。
- [ ] Codex 不可用时使用顺序选择作为降级方案。
- [x] 首次学习提交与 D1/D3/D7/D14/D21 创建放在同一事务。
- [x] 重复调用返回同一会话，不重复推进状态。

### 8.3 到期复习任务

- [x] 创建每天唯一的 `review` 会话。
- [x] 查询今日到期和此前逾期轮次。
- [x] 同一词同一天只生成一个复习项目。
- [x] 保存本轮答案、评分、反馈和完成时间。
- [x] 合并覆盖同一天已到期的重复轮次。
- [x] 完成 D21 后写入 `completed` 或 `expired_unmastered`。
- [x] 周末不创建新会话。

### 8.4 状态机测试

- [ ] 使用 fake clock 模拟完整 21 天周期。
- [x] 覆盖周五首次学习后 D1/D3 重合场景。
- [ ] 覆盖任务漏做后变成逾期的场景。
- [ ] 覆盖重复请求、服务重启和事务回滚。
- [ ] 覆盖文档末尾不足 6 个词的场景。

完成标准：状态机在任意一天都能给出唯一、可解释、可重复的结果。

## 9. M5：实现本地 API

### 9.1 基础服务

- [x] Fastify 只监听 `127.0.0.1`。
- [x] 实现统一错误格式、请求日志和 request ID。
- [ ] 实现 `/api/health`，包含数据库和词库可读状态。
- [x] 实现 CORS 和本地来源限制。

### 9.2 词库和会话接口

- [x] `POST /api/import`
- [x] `POST /api/sessions/new/today`
- [x] `GET /api/sessions/new/today`
- [x] `POST /api/sessions/review/today`
- [x] `GET /api/sessions/review/today`
- [x] `GET /api/reviews/queue`
- [ ] `POST /api/reviews/:id/answer`
- [ ] `POST /api/reviews/:id/rating`
- [x] `GET /api/history`

### 9.3 练习和评测接口

- [ ] `POST /api/exercises/:id/submit`
- [ ] `GET /api/exercises/:id/feedback`
- [ ] 定义 `pending_evaluation` 状态和轮询行为。
- [ ] 所有写接口实现幂等键或唯一约束保护。

完成标准：通过 API 集成测试完成一次新词会话和一次复习会话，无需前端参与。

## 10. M6：实现最小前端闭环

### 10.1 应用外壳

- [x] 建立“新词学习、到期复习、学习历史”三个主视图。
- [x] 显示服务未启动、数据库错误和词库解析错误。
- [x] 加入加载、空状态、失败重试和保存成功状态。
- [x] 适配 Obsidian 内嵌宽度和普通浏览器宽度。

### 10.2 新词学习页面

- [x] 显示当天情景主题和学习进度。
- [x] 单词卡默认只显示英文。
- [ ] 用户输入释义后才能展开中文和音标。
- [x] 提供互斥的四档评分控件。
- [ ] 提供拼写题和逐字符差异提示。
- [ ] 完成会话后显示首次学习和复习计划摘要。

### 10.3 到期复习页面

- [x] 显示今日到期、逾期和即将到期数量。
- [ ] 默认隐藏答案，完成主动回忆后再显示。
- [ ] 显示来源文档、轮次和日期的可展开详情。
- [x] 同一个词当天只展示一次。
- [x] 提交后即时更新剩余数量。

### 10.4 历史页面

- [x] 按日期显示新词和复习会话。
- [ ] 查看单词的完整五轮记录。
- [ ] 区分 `completed` 和 `expired_unmastered`。

完成标准：用户只通过浏览器即可完成两类任务，刷新和重启后进度不丢失。

## 11. M7：Codex 生成和开放式评测

### 11.1 适配器边界

- [x] 定义与具体模型无关的 `ContentGenerator` 接口。
- [x] 定义 `AnswerEvaluator` 接口。
- [x] 实现确定性的测试适配器，测试不调用模型。
- [ ] 记录模型、版本、请求时间和生成结果 ID。
- [ ] 限制发送给模型的数据，只包含当前目标词和当前答案。

### 11.2 情景生成

- [ ] 让 Codex 从当前文档未学词中选择最多 6 个语义相容词。
- [ ] 校验返回 ID，不接受越过当前文档或已学词。
- [ ] 生成一个短文或两个较短场景。
- [ ] 对无法自然编入短文的词生成独立例句。
- [ ] 生成参考翻译，但答题前不返回给前端。
- [ ] 保存提示词版本和完整结构化结果。

### 11.3 答案评测

- [ ] 本地即时评测拼写和选择题。
- [ ] 使用 Codex 评测释义、造句和短文翻译。
- [ ] 返回评分、简短反馈、纠正内容和建议答案。
- [ ] 评测失败时保存用户答案并支持重试。
- [ ] 用户可以覆盖系统建议评分，但必须保留两者记录。

完成标准：断开模型时核心学习和复习仍可运行；恢复后可以补做待评测任务。

## 12. M8：Obsidian 集成和 Markdown 输出

### 12.1 Obsidian 学习入口

- [x] 在 Obsidian 创建固定学习入口 Markdown。
- [x] 嵌入 `http://127.0.0.1:<port>` 本地应用。
- [ ] 显示服务未启动时的明确提示。
- [x] 验证 Obsidian 阅读视图、窗口缩放和主题切换。

### 12.2 阅读归档

- [x] 从 SQLite 生成 `study/reports/YYYY-MM-DD.md`。
- [ ] 报告包含新词、复习、短文、答案、评分和反馈。
- [x] 重复导出同一天产生稳定内容。
- [x] 修改报告不影响数据库状态。

### 12.3 待复习快照

- [x] 从 SQLite 生成 `study/review-queue.md`。
- [x] 显示逾期、今日到期和近期将到期词条。
- [ ] 每次新词、复习和手动刷新后重新生成。
- [x] 删除快照后可从 SQLite 完整重建。

完成标准：用户在 Obsidian 中能进入应用，并能搜索每日归档和待复习快照。

## 13. M9：工作日自动化

### 13.1 本地服务生命周期

- [ ] 决定使用 `launchd` 常驻服务或按需启动。
- [ ] 添加启动、停止、状态和日志命令。
- [ ] 服务异常退出后能够恢复，且不会启动多个实例。

### 13.2 两个独立任务

- [ ] 创建周一至周五的新词学习任务。
- [ ] 创建周一至周五的到期复习任务。
- [ ] 周末调用返回无需执行，不创建空会话。
- [ ] 任务重复运行时返回同一会话。
- [ ] 失败时提供可读通知和重试入口。

### 13.3 Codex 自动化

- [ ] 仅在需要生成或语义评测时调用 Codex。
- [ ] 任务提示词只传递当前所需数据。
- [ ] 保存每次自动化运行结果和错误摘要。
- [ ] 验证 Codex 不可用时的降级行为。

完成标准：连续五个工作日自动运行，无重复会话、重复复习或状态丢失。

## 14. M10：备份、恢复和真实词库试运行

### 14.1 SQLite 运维

- [x] 使用 SQLite backup API 生成一致性备份。
- [ ] 实现备份保留和过期清理。
- [ ] 实现只读数据库检查和 migration 前备份。
- [ ] 编写恢复流程并实际演练一次。

### 14.2 真实数据验证

- [ ] 对真实词库执行 dry-run，核对文件和词条数量。
- [ ] 抽查每页首、中、末位置 ID 与原 Markdown 一致。
- [ ] 验证重复英文词被保存为不同来源词条。
- [x] 使用测试数据库完成一轮新词选择和情景生成。
- [ ] 使用虚拟日期跑完至少一组完整 21 天周期。
- [x] 验证不会改写 `english-words*.md`。

### 14.3 发布前检查

- [ ] 单元、集成和端到端测试全部通过。
- [x] TypeScript、Biome 和构建检查全部通过。
- [ ] 检查日志和数据库中没有 API 密钥。
- [ ] 检查所有写接口的幂等行为。
- [ ] 更新架构文档中已经确定的待决策项。

完成标准：可以使用真实词库开始学习，并能从备份恢复到相同状态。

## 15. 推荐开发顺序

严格按以下顺序推进，每个阶段通过验收后再进入下一阶段：

```text
M0 参数确认
  -> M1 工程初始化
  -> M2 领域模型和日期规则
  -> M3 Markdown + SQLite
  -> M4 状态机
  -> M5 API
  -> M6 最小前端
  -> M7 Codex 生成与评测
  -> M8 Obsidian 集成
  -> M9 自动化
  -> M10 真实数据与恢复验证
```

第一个可演示版本应在 M6 完成：使用本地测试适配器生成固定情景，不等待 Codex 接入。这样可以先验证学习流程和状态设计，再引入模型不确定性。
