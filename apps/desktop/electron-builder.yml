appId: com.enpet.app
productName: EnPet
directories:
  output: release
files:
  - dist/**
  - package.json
extraResources:
  - from: ../web/dist
    to: web-dist
asar: true
npmRebuild: false
mac:
  category: public.app-category.education
  identity: null
  target:
    - target: dmg
      arch:
        - arm64
        - x64
publish:
  provider: github
  # 默认是 draft，需要手动点 Publish；改成 release 后推 v* tag 直接正式发布
  releaseType: release
